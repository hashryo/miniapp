  <div class="contents_row" >
    <div class="username"><p><%= @nickname %></p></div>
    <% @tweets.each do |tweet| %>
      <div class="content_post" style="background-image: url(<%= tweet.image %>);">
        <%= simple_format(tweet.prefectures) %>
      </div>
    <% end %>
  </div>