.contents_row
  .contents_row_left
    - @tweets.each do |tweet|
      = render partial: "tweet", locals: { tweet: tweet }

  .contents_row_right
    .ranking      
      .ranking_title
        お気に入り数ランキング
      - @ranking.each.with_index(1) do |user,index|
        .user
          = index 
          位・・・
          = user.nickname